server.port=8080

# DB local
spring.datasource.url=jdbc:postgresql://localhost:5432/agenda_db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Actuator: expón health/info
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never

# CORS global permisivo SOLO en dev
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000,android-app://com.umb.agenda
